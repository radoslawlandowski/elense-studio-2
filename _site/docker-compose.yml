services:
  jekyll:
    image: jekyll/jekyll:latest
    volumes:
      - .:/srv/jekyll
    ports:
      - "4000:4000"
    command: sh -c "bundle install && bundle exec jekyll serve --force_polling --livereload --host 0.0.0.0"
